# Servers
#
# There must only be a single master node to initialize the
# cluster, if multiple cp nodes should join the cluster, they
# should be added to the k8s_cp group.
[k8s_master]
192.168.178.101 name=cp1 # TODO schedule_pods=true

[k8s_cp]
192.168.178.102 name=cp2

[k8s_worker]

[all:children]
k8s_master
k8s_cp
k8s_worker

[k8s_master:vars]
role=master

[k8s_cp:vars]
role=cp

[k8s_worker:vars]
role=worker
